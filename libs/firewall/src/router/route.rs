use crate::{Firewall, RouteParams};


impl Firewall {
    pub fn load(mut self) {
        todo!("load routing flow");
    }

    pub fn save(self) {
        todo!("save routing flow");
    }
}


impl Firewall {
    pub fn create(mut self, route: String, params: RouteParams) {
        todo!("Route creating flow");
    }

    pub fn get_route_list(self) {
        todo!("Route list flow");
    }

    pub fn update(mut self, route: String, params: RouteParams) {
        todo!("Route updating flow");
    }

    pub fn remove(mut self, route: String) {
        todo!("remove route flow");
    }
}

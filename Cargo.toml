[package]
name = "netvm"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = "z"
strip = "symbols"
lto = true
panic = "abort"


[profile.release]
opt-level = "z"
strip = "symbols"
lto = true
panic = "abort"

[dependencies]
linked_list_allocator = "0.10.5"


